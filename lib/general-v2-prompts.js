// ╔══════════════════════════════════════════════════════════╗
// ║ general-v2-prompts.js — 내과 V2 프롬프트 (1차 진료 허브)   ║
// ║ Purpose: 증상/검진 이상 → 기본검사 → 원인 범위 확인        ║
// ║          → 전문내과 연계 판단                              ║
// ║ ⚠ 관측 전. FREEZE 아님.                                    ║
// ║ 톤: 정보형·비1인칭·의료광고법 안전.                        ║
// ║ 핵심 제약: 질환 확정형 서술 금지. 연계 허브 역할만.        ║
// ╚══════════════════════════════════════════════════════════╝

export const GENERAL_SYSTEM_PROMPT = `당신은 내과(1차 진료) 정보 안내 글을 작성합니다.

[역할 정의 — 가장 중요]
이 내과는 '1차 진료 허브'입니다. 질환을 확정하는 곳이 아니라,
증상이나 검진 이상 소견을 기본검사로 확인하고, 원인 범위를 좁혀
필요하면 전문 진료과로 연계 여부를 판단하는 창구입니다.

[Purpose 축 — 모든 섹션이 이 흐름 위에 있어야 합니다]
증상 또는 검진 이상 → 기본 문진·검사 → 원인 범위 확인 → 전문내과 연계 판단

[절대 금지 — 질환 확정형 서술]
❌ "당뇨입니다" / "갑상선질환입니다" / "협심증입니다" / "천식입니다"
❌ 특정 질환의 치료·약물·합병증을 상세히 설명하는 것
✅ "혈당 수치에 따라 내분비내과 연계 여부를 판단합니다"
✅ "심전도 소견에 따라 순환기내과 확인이 검토될 수 있습니다"

전문 질환의 상세 진단·치료는 각 전문내과(소화기·호흡기·순환기·내분비)의 영역입니다.
일반내과 글에서는 '어디까지 확인하고, 어디서부터 넘기는가'만 다룹니다.

[연계 대상 과 — 진료별로 다릅니다]
연계 방향은 각 글의 [연계 방향] 항목에 명시된 과만 언급합니다.
모든 글에 내분비·순환기를 붙이지 마십시오. 진료와 무관한 과를 끌어오면 오류입니다.
(예: 대상포진 → 통증의학과·피부과·안과 / 기침 → 호흡기내과 / 복통 → 소화기내과)

[지역명 표기]
지역명은 문장 안에서 조사와 함께 자연스럽게 씁니다. 지역명 없이 조사만 남기지 마십시오.
❌ "에서 진료를 검토할 수 있습니다"
✅ "가까운 내과에서 진료를 검토할 수 있습니다"

[작성 규칙]
- 정보형 문체. 1인칭 후기·경험담 금지 ("~했어요", "제가", "저는" 금지)
- 진단·효과 단정 금지 ("완치", "확실히", "무조건", "정상입니다")
- 광고 어휘 금지 ("최고", "강력 추천", "꼭 받으세요", "후회 없")
- AI 논문투 금지 ("정리하면", "결론적으로", "따라서", "체계적인", "살펴보겠습니다")
- 병원명·의료진명 직접 노출 금지
- 각 섹션은 '## 섹션명'으로 시작하고, 해당 섹션 내용만 작성 (단, 첫 섹션은 소제목 없이 본문으로 시작) // [세션40][NOHDR-01]
- 마크다운 볼드·리스트 남용 금지. 자연스러운 문단으로.`;

export const GENERAL_FORBIDDEN = [
  "완치", "완벽", "확실히", "무조건", "반드시 낫",
  "정상입니다", "이상 없습니다", "진단됩니다", "효과 보장",
  "최고", "강력 추천", "후회 없", "꼭 받으세요",
  "정리하면", "결론적으로", "따라서", "체계적인", "살펴보겠습니다",
];

export function buildGeneralPrompt(section, treatment, region, options = {}) {
  const name      = treatment.name;
  const baseExam  = treatment.baseExam  || "문진 · 기본 혈액검사";
  const referHint = treatment.referHint || "확인된 범위에 따라 전문 진료과 연계 여부 판단";
  const compare   = treatment.compareWith || "다른 접근";

  switch (section) {
    case 'concern':  return P_concern(name, region);
    case 'visit':    return P_visit(name, region);
    case 'exam':     return P_exam(name, region, baseExam);
    case 'refer':    return P_refer(name, region, referHint);
    case 'choice':   return P_choice(name, region);
    case 'process':  return P_process(name, region, baseExam);
    case 'closing':  return P_closing(name, region, referHint);
    default:
      throw new Error(`[general-v2-prompts] 알 수 없는 섹션: ${section}`);
  }
}

// ① 도입 — 상황 확인
function P_concern(name, region) {
  return `[세션40][NOHDR-01] ⚠ 소제목(##) 출력 금지. 본문부터 바로 시작합니다.

[주제] ${name}으로 내과 진료를 검토하게 되는 상황

[조건]
- 어떤 상황에서 이 진료를 검토하게 되는지 2~3가지를 담담하게 제시
- 과장·불안 조성 금지. "위험합니다", "방치하면" 같은 표현 금지
- 홍보성 문장 금지 ("~에서는 ~기회를 제공받을 수 있습니다" 같은 표현 금지)
- "${region}"을 1회 자연스럽게 포함
- 분량: 200~280자`;
}

// ② 진료 고려 상황
function P_visit(name, region) {
  return `## 이럴 때 진료를 고려해볼 수 있습니다

[주제] ${name}에서 내과 진료가 검토되는 구체적 조건

[조건]
- 증상의 지속 기간, 동반 상황, 검진 결과 등 구체적 조건 서술
- 이 진료가 '원인을 좁혀가는 1차 확인 단계'임을 자연스럽게 드러낼 것
- 특정 질환명을 확정적으로 언급하지 말 것 (가능성 범위로만 언급)
- 분량: 220~320자`;
}

// ③ 기본검사 — 무엇을 확인하는가
function P_exam(name, region, baseExam) {
  return `## 진료에서는 무엇을 확인하나요?

[주제] ${name}에서 시행되는 기본 확인 항목

[참고 — 기본 확인 항목]
${baseExam}

[조건]
- 위 항목을 자연스러운 문장으로 풀어 설명 (나열식 금지)
- 각 검사가 '무엇을 배제하거나 확인하기 위한 것인지' 목적 중심으로 서술
- 이 단계는 원인 범위를 좁히는 단계임을 명확히 할 것
- 검사 결과를 확정 진단으로 연결하지 말 것
- 분량: 250~360자`;
}

// ④ 전문내과 연계 기준 — ★ 핵심 섹션 (허브 역할)
function P_refer(name, region, referHint) {
  return `## 전문 진료로 연계되는 기준

[주제] ${name}에서 전문 진료과 연계가 검토되는 기준

[참고 — 연계 방향]
${referHint}

[조건 — 이 섹션이 이 글의 핵심입니다]
- ⚠ 연계 대상 과는 위 [참고 — 연계 방향]에 적힌 과만 언급할 것. 그 외 과 언급 금지.
  (예: 대상포진 글에 내분비내과·순환기내과가 나오면 오류)
- 기본검사에서 어떤 소견이 확인되면 해당 과 확인이 검토되는지 서술
- "필요 시 ~에서 추가 확인이 검토될 수 있습니다" 형태의 조건부 서술
- 반드시 전문과로 가야 한다는 단정 금지. 판단 기준만 제시
- 반대로 기본검사 범위에서 관리가 이어지는 경우도 함께 언급
- 분량: 250~360자`;
}

// ⑤ 병원 선택 기준
function P_choice(name, region) {
  return `## 병원 선택 시 확인할 점

[주제] ${name} 진료 시 확인하면 좋은 점

[조건]
- 1차 진료 허브 특성에 맞는 기준으로 서술
  예) 기본검사 항목이 갖춰져 있는지 / 이전 검사 결과와 이어서 볼 수 있는지 /
      전문 진료과 연계가 필요할 때 안내가 이루어지는지 / 추적 간격 안내가 명확한지
- 특정 병원 홍보 금지. 확인 기준만 제시
- "${region}"을 1회 자연스럽게 포함
- 분량: 220~320자`;
}

// ⑥ 진료 진행 과정
function P_process(name, region, baseExam) {
  return `## 진료실에서 확인하는 과정

[주제] ${name} 진료의 실제 진행 흐름

[참고 — 확인 항목]
${baseExam}

[조건]
- 접수 → 문진 → 기본검사 → 결과 확인 → 이후 방향 논의 순서로 담담하게 서술
- 검사 방식이나 결과 확인 시점은 상황에 따라 다를 수 있음을 언급
- 비용은 "항목에 따라 안내됩니다" 수준으로만 (금액 금지)
- 분량: 250~360자`;
}

// ⑦ 마무리
function P_closing(name, region, referHint) {
  return `## 마무리

[주제] ${name} 진료의 위치와 이후 방향

[조건]
- 이 진료가 '원인 범위를 확인하고 다음 방향을 정하는 단계'임을 한 번 더 정리
- 같은 소견이라도 상태에 따라 이후 접근이 달라질 수 있음을 언급
- 필요 시 전문 진료과 확인이 이어질 수 있음을 조건부로 언급
- CTA·권유·홍보 금지. 정보 정리로 마무리
- 분량: 180~260자`;
}

// 이미지 ALT
export function getGeneralImageAlts(treatment, region) {
  const n = treatment.name;
  return {
    concern: `[이미지: ${region} ${n} 안내 | ${n} 관련 상황 정보]`,
    visit:   `[이미지: ${region} 내과 ${n} 진료 고려 상황 | 내원 안내]`,
    exam:    `[이미지: ${region} ${n} 확인 항목 | 기본검사 과정]`,
    refer:   `[이미지: ${region} 내과 전문 진료 연계 기준 | 연계 판단 안내]`,
    choice:  `[이미지: ${region} ${n} 병원 선택 안내 | 확인할 점 정보]`,   // ★ BUGFIX 2026-07-14 — region/name 누락 하드코딩 수정
    process: `[이미지: ${region} ${n} 진료실 확인 과정 | 진행 안내]`,
    closing: `[이미지: ${region} ${n} 정보 | 내과 안내]`,
  };
}

export function renderInfoBlocks(treatment) {
  return treatment.baseExam || "";
}
